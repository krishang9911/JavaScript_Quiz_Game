# Prompt Quizzer – JavaScript Quiz Game

## 📌 Project Overview
Prompt Quizzer is a simple JavaScript-based quiz game that runs entirely in the browser console.  
It uses `prompt()` to collect user input and `alert()` to give immediate feedback after each question.

This project was created as part of **Web Development I – Lab Assignment 4** and focuses on practicing core JavaScript concepts without using HTML or DOM manipulation.

---

## ✅ Features
- Predefined quiz questions stored in an array
- Questions and answers stored as objects
- Input taken using `prompt()`
- Immediate feedback using `alert()`
- Case-insensitive answer checking using `toLowerCase()`
- Extra space removal using `trim()`
- Score tracking throughout the quiz
- Final score displayed at the end

---

## 🧠 JavaScript Concepts Used
- Arrays
- Objects
- Functions
- `for` loops
- Conditional statements (`if / else`)
- String methods (`toLowerCase()`, `trim
